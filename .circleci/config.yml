version: 2.1
orbs:
  android: circleci/android@2.5.0
jobs:
  build:
    executor:
      name: android/android-machine
      resource-class: large
      tag: default
    steps:
      - checkout
      - run: java --version
      - restore_cache:
          key: android-orb-v1-
      - android/change-java-version:
          java-version: 17
          
      # Konfigurasi memory yang optimal
      - run:
          name: Configure Build Settings
          command: |
            mkdir -p ~/.gradle
            echo "org.gradle.jvmargs=-Xmx8g" >> ~/.gradle/gradle.properties
            echo "org.gradle.daemon=false" >> ~/.gradle/gradle.properties
            echo "org.gradle.parallel=true" >> ~/.gradle/gradle.properties
            echo "org.gradle.caching=true" >> ~/.gradle/gradle.properties
            echo "android.builder.sdkDownload=true" >> ~/.gradle/gradle.properties
      
      - run:
          name: Chmod permissions
          command: sudo chmod +x ./gradlew
          
      - run:
          name: Clean Project
          command: ./gradlew clean --info
          
      - run:
          name: Download Dependencies
          command: ./gradlew androidDependencies
          
      - save_cache:
          key: 'android-orb-v1-{{ epoch }}'
          paths:
            - ~/.android/build-cache
            - ~/.android/cache
            - ~/.gradle
            
      # Build dengan debugging info
      - run:
          name: Build Release with Debug Info
          command: |
            ./gradlew assembleRelease \
            -x lint \
            --info \
            --stacktrace \
            --scan
          
      - store_artifacts:
          path: app/build/reports
          destination: reports
          
      - store_artifacts:
          path: app/build/outputs/apk/release/
          destination: release-apk

workflows:
  version: 2
  build_and_test:
    jobs:
      - build